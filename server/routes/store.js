const express = require('express');
const router = express.Router();
const multer = require('multer');
const { Store } = require("../models/Store");

//=================================
//             Store
//=================================


router.post('/store/owner', (req, res) => {

  

})

module.exports = router;